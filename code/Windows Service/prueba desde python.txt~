from socket import *
from threading import Thread
import sys

HOST = 'localhost'
PORT = 7388
BUFSIZE = 1024
ADDR = (HOST, PORT)

tcpCliSock = socket()
tcpCliSock.connect(ADDR)

def recv():
    while True:
        data = tcpCliSock.recv(BUFSIZE)
        if not data: sys.exit(0)
        print data

Thread(target=recv).start()


while (True):
    msge = 'C:\Program Files (x86)\A new service test\SustantivosFinal.multik'
    tcpCliSock.send(msge)
    tcpCliSock.send('1/1')